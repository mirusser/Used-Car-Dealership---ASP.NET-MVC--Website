@model IEnumerable<TypicalMirek_UsedCarDealer.Models.Car>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdditionalData.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Body.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MainData.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Propulsion.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SourceOfEnergy.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdditionalData.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Body.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MainData.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Propulsion.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourceOfEnergy.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
