@model IEnumerable<TypicalMirek_UsedCarDealer.Models.Character>

@{
    ViewBag.Title = "Index";
    Layout = "../Shared/_AdminLayout.cshtml";
}

@using (Html.BeginForm("List", "Characters", null, FormMethod.Get))
{
    <h1 style="text-align: center">List of characters</h1>

    <p>
        @Html.ActionLink("New character", "Create", "Characters", new { @class = "btn btn-primary btn-product col-md-12 col-xs-12" })
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", "Characters", new { Id = item.Id }, new { @class = "btn btn-info btn-xs col-md-4 col-xs-12" })
                    @Html.ActionLink("Details", "Details", "Characters", new { Id = item.Id }, new { @class = "btn btn-info btn-xs col-md-4 col-xs-12" })
                    @Html.ActionLink("Delete", "Delete", "Characters", new { Id = item.Id }, new { @class = "btn btn-danger btn-xs col-md-4 col-xs-12" })
                </td>
            </tr>
        }

    </table>
}